# -student_data.csv
Student Database Management System in Python using CSV file handling. It supports adding, viewing, searching, updating, and deleting student records. Prevents duplicate roll numbers and displays data in tabular format. A simple project demonstrating CRUD operations and file management.
